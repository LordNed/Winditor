<UserControl x:Class="WindEditor.View.WDetailTree"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WindEditor.View"
             xmlns:vm="clr-namespace:WindEditor.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="200">
    <UserControl.DataContext>
        <vm:WDetailsViewViewModel/>
    </UserControl.DataContext>
    <Grid>
        <DockPanel LastChildFill="True">
            <TreeView x:Name="WDetailTreeView" ItemsSource="{Binding Path=Categories}">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Path=Value}">
                        <DockPanel LastChildFill="True">
                            <local:WDetailCategoryRow DataContext="{Binding Path=Value}"/>
                        </DockPanel>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </DockPanel>
    </Grid>
</UserControl>
